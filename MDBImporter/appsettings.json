{
  // 数据库连接字符串配置
  "ConnectionStrings": {
    // SQL Server 数据库连接字符串
    "SqlServer": "server=175.178.66.226,8043;uid=sa;pwd=DATA123456!;database=TCPUADATA;TrustServerCertificate=True;"
  },

  // 应用程序设置
  "ApplicationSettings": {
    // MDB 数据库访问提供程序（用于访问Access数据库）
    "MDBProvider": "Microsoft.ACE.OLEDB.12.0",
    // 是否自动检测MDB数据库提供程序
    "UseAutoDetectProvider": true,
    // MDB数据库密码（如果Access数据库有密码保护）
    "MDBPassword": "",
    // 日志保留天数
    "LogRetentionDays": 30,
    // 最大批量处理大小
    "MaxBatchSize": 1000,
    // 是否启用自动同步
    "EnableAutoSync": true,
    // 默认同步间隔（分钟）
    "DefaultSyncInterval": 5,
    // 是否启用文件监视器
    "EnableFileWatcher": true,

    // 网络计算机配置列表
    "NetworkComputers": [
      {
        // 计算机名称
        "ComputerName": "MENGZHOU",
        // IP地址
        "IPAddress": "127.0.0.1",
        // MDB文件所在文件夹路径
        "MDBFolder": "D:\\MDBFiles\\Source",
        // 是否启用该计算机的同步
        "Enabled": true,
        // 描述信息
        "Description": "本地电脑",
        // 该计算机的同步间隔（分钟）
        "SyncIntervalMinutes": 5
      },
      {
        "ComputerName": "QINYANG",
        "IPAddress": "127.0.0.1",
        "MDBFolder": "D:\\MDBFiles\\A",
        "Enabled": true,
        "Description": "QINYANG",
        "SyncIntervalMinutes": 5
      }
    ]
  },

  // 批量复制设置（用于SQL批量插入操作）
  "BulkCopySettings": {
    // 每批处理的行数
    "BatchSize": 1000,
    // 超时时间（秒）
    "TimeoutSeconds": 300,
    // 每处理多少行后通知进度
    "NotifyAfter": 1000,
    // 是否启用流式处理
    "EnableStreaming": true
  },

  // 日志记录配置
  "Logging": {
    // 日志级别设置
    "LogLevel": {
      // 默认日志级别
      "Default": "Information",
      // Microsoft相关日志的级别
      "Microsoft": "Warning",
      // 宿主生命周期相关日志级别
      "Microsoft.Hosting.Lifetime": "Information",
      // MDBImporter自定义组件的日志级别
      "MDBImporter": "Information"
    },
    // 控制台日志配置
    "Console": {
      "LogLevel": {
        "Default": "Information",
        "Microsoft": "Warning"
      }
    }
  }
}